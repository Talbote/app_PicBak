{{ form_start(form) }}
        {{ form_widget(form) }}
<input type="submit" value="{{ submitButtonText|default('Create Picture') }}"  class="btn btn-primary"formnovalidate>
{{ form_end(form) }}
<a href="{{ path('app_pictures_index') }}"  class="btn">Back</a>