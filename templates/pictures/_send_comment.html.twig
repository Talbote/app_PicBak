<div class="mb-5">
    {{ form_start(commentForm, {'attr': {'id': 'send-comment-form'}}) }}
    <div class="row">
        {{ form_errors(commentForm) }}
    </div>

    <div class="row">

        <div class="col-8">
            {{ form_widget(commentForm.textComment) }}
        </div>

        <div class="col-2 " id="some-custom-id">
            <button id="submit-comment-form" class="btn btn-light btn-sm"><span
                        class="text-muted far fa-paper-plane"></span></button>
        </div>
    </div>
    {{ form_end(commentForm) }}
</div>